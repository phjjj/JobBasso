import { pageStyles } from '@/app/page.css';

const Page = () => {
  return (
    <div className={pageStyles.container}>
      <h1>Hello, world!</h1>
      <p>This is a sample page using Vanilla Extract for styling.</p>
    </div>
  );
};

export default Page;
